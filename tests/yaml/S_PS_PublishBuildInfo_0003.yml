resources:
  - name: S_PS_PublishBuildInfo_0003_output
    type: BuildInfo
    configuration:
      sourceArtifactory: s_artifactory
      buildName: 'name'
      buildNumber: '1'

pipelines:
  - name: pipeline_S_PS_PublishBuildInfo_0003
    steps:
      - name: S_PS_PublishBuildInfo_0003_1
        type: PowerShell
        configuration:
          nodePool: win_2019
          integrations:
            - name: s_artifactory
        execution:
          onExecute:
            - echo "${run_id}" > S_PS_PublishBuildInfo_0003.txt
            - $jfrog_cli_path rt upload S_PS_PublishBuildInfo_0003.txt test-automation-generic-local
            - add_run_files (Join-Path ([System.IO.Path]::GetTempPath()) jfrog) jfrog
            - add_run_variables buildName=${JFROG_CLI_BUILD_NAME} buildNumber=${JFROG_CLI_BUILD_NUMBER}
      - name: S_PS_PublishBuildInfo_0003_2
        type: PublishBuildInfo
        configuration:
          nodePool: win_2019
          inputSteps:
            - name: S_PS_PublishBuildInfo_0003_1
